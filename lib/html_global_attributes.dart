import 'package:web/web.dart';

// Hypertext Markup Language Global Attributes

Attr accessKey = document.createAttribute('accesskey');

Attr autoCapitalize = document.createAttribute('autocapitalize');

Attr autoFocus = document.createAttribute('autofocus');

Attr classAttribute = document.createAttribute('class');

Attr contentEditable = document.createAttribute('contenteditable');

Attr textDirection = document.createAttribute('dir');

Attr draggable = document.createAttribute('draggable');

Attr enterKeyHint = document.createAttribute('enterkeyhint');

Attr exportParts = document.createAttribute('exportparts');

Attr hidden = document.createAttribute('hidden');

Attr identifier = document.createAttribute('id');

Attr inert = document.createAttribute('inert');

Attr inputMode = document.createAttribute('inputmode');

Attr isAttribute = document.createAttribute('is');

Attr itemIdentifier = document.createAttribute('itemid');

Attr itemProperties = document.createAttribute('itemprop');

Attr itemReference = document.createAttribute('itemref');

Attr itemScope = document.createAttribute('itemscope');

Attr itemType = document.createAttribute('itemtype');

Attr language = document.createAttribute('lang');

Attr numberUsedOnce = document.createAttribute('nonce');

Attr elementPart = document.createAttribute('part');

Attr popover = document.createAttribute('popover');

Attr elementSlot = document.createAttribute('slot');

Attr spellCheck = document.createAttribute('spellcheck');

Attr elementStyle = document.createAttribute('style');

Attr tabIndex = document.createAttribute('tabindex');

Attr elementTitle = document.createAttribute('title');

Attr translate = document.createAttribute('translate');

Attr writingSuggestions = document.createAttribute('writingsuggestions');
