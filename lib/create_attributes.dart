import 'package:web/web.dart';

// Attribute Creation Functions for Hypertext Markup Language

Attr createAccept = document.createAttribute('accept');

Attr createAutoComplete = document.createAttribute('autocomplete');

Attr createCapture = document.createAttribute('capture');

Attr createCrossOrigin = document.createAttribute('crossorigin');

Attr createDirectionName = document.createAttribute('dirname');

Attr createDisabled = document.createAttribute('disabled');

Attr createElementTiming = document.createAttribute('elementtiming');

Attr createForAttribute = document.createAttribute('for');

Attr createMaximumValue = document.createAttribute('max');

Attr createMaximumLength = document.createAttribute('maxlength');

Attr createMinimumValue = document.createAttribute('min');

Attr createMinimumlength = document.createAttribute('minlength');

Attr createMultiple = document.createAttribute('multiple');

Attr createPattern = document.createAttribute('pattern');

Attr createPlaceHolder = document.createAttribute('placeholder');

Attr createReadOnly = document.createAttribute('readonly');

Attr createResourceRelationship = document.createAttribute('rel');

Attr createRequiredAttribute = document.createAttribute('required');

Attr createSize = document.createAttribute('size');

Attr createSteppingInterval = document.createAttribute('step');
