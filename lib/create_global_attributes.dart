import 'package:web/web.dart';

// Attribute Creators for the Document Object Model

Attr createAccessKey = document.createAttribute('accesskey');

Attr createAutoCapitalize = document.createAttribute('autocapitalize');

Attr createAutoFocus = document.createAttribute('autofocus');

Attr createClassAttribute = document.createAttribute('class');

Attr createContentEditable = document.createAttribute('contenteditable');

Attr createTextDirection = document.createAttribute('dir');

Attr createDraggable = document.createAttribute('draggable');

Attr createEnterKeyHint = document.createAttribute('enterkeyhint');

Attr createExportParts = document.createAttribute('exportparts');

Attr createHidden = document.createAttribute('hidden');

Attr createIdentifier = document.createAttribute('id');

Attr createInert = document.createAttribute('inert');

Attr createInputMode = document.createAttribute('inputmode');

Attr createIsAttribute = document.createAttribute('is');

Attr createItemIdentifier = document.createAttribute('itemid');

Attr createItemProperties = document.createAttribute('itemprop');

Attr createItemReference = document.createAttribute('itemref');

Attr createItemScope = document.createAttribute('itemscope');

Attr createItemType = document.createAttribute('itemtype');

Attr createLanguageAttribute = document.createAttribute('lang');

Attr createNumberUsedOnce = document.createAttribute('nonce');

Attr createPartAttribute = document.createAttribute('part');

Attr createPopover = document.createAttribute('popover');

Attr createSlotAttribute = document.createAttribute('slot');

Attr createSpellCheck = document.createAttribute('spellcheck');

Attr createStyleAttribute = document.createAttribute('style');

Attr createTabIndex = document.createAttribute('tabindex');

Attr createTitleAttribute = document.createAttribute('title');

Attr createTranslate = document.createAttribute('translate');

Attr createWritingSuggestions = document.createAttribute('writingsuggestions');
